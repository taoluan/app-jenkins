pipeline {
  agent any
  environment {
    MVN_HOME = "Maven 3.6"
  }
  stages {
    stage('SCM') {
      steps {
        checkout scm
      }
    }

    stage('SonarQube Analysis') {
      steps {
        withSonarQubeEnv('Scan') {
            sh "${MVN_HOME}/bin/mvn clean verify sonar:sonar -Dsonar.projectKey=jenkins -Dsonar.projectName='jenkins'"
        }
      }
    }
  }
}
